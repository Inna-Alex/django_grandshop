{% load catalog_tags %}{% for row in data %}{% autoescape off %}"{{ row.0|addslashes }}", "{{ row.1|addslashes }}", "{{ row.2|addslashes }}", "{{ row.3|addslashes }}", "{{ row.4|addslashes }}", "{{ row.5|addslashes|csv_bool_str }}", "{{ row.6|addslashes }}"
{% endautoescape %}{% endfor %}